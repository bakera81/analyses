# Helpful hints
- When dealing with large datasets, be sure to set `cache: false` in the YAML header of the QMD file.
- Shiny apps are hosted on shinyapps.io
- To update, render everything locally and commit.